// import App from './App';
// import { render, screen } from '@testing-library/react';

// test('Heading should be BecomePro', () => {
//   render(<App />);

//   const headingElement = screen.getByText('BecomePro');

//   expect(headingElement).toBeInTheDocument();
// });
